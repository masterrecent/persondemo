# 知序 · 多 Agent 个人学习助理 — 项目说明

本文档说明项目的定位、技术栈、目录结构、每个文件的职责、启动方式、接口清单，以及一次请求从浏览器到数据库的完整执行链路。

---

## 一、项目定位

一个基于 **LangGraph 多 Agent** 的个人学习与生活助理，把「制定计划 → 每日执行 → 打卡专注 → 数据复盘 → 知识问答」串成闭环。核心能力分两类：

**对话式能力**（由 `supervisor` 路由，共 3 条分支）：

- `plan`：学习规划 —— 把「我要两周学完 LangGraph」这类目标拆成逐日任务，写入 SQLite。
- `schedule`：日程查询 —— 回答「今天该学什么」，读取 SQLite 中今天对应的任务。
- `knowledge`：知识库问答 —— 基于 FAISS 向量库做 RAG 检索，并用大模型复核相关性后生成回答。

**页面式能力**（侧边栏 7 个视图）：

| 视图 | 说明 |
|---|---|
| AI 助理 | 对话主界面，展示回复、来源标签与可展开的 Agent 执行链路 |
| 完整学习计划 | 按计划 → 周 → 每日任务三级浏览 |
| 今日安排 | 今日任务清单，勾选完成并强制填写 1–5 分评价 |
| 我的课表 | 每周课程表，支持链接 / ICS 文件 / 手动三种方式导入 |
| 专注计时 | 专注与休息计时，记录写入 SQLite，前端用 localStorage 持久化计时状态 |
| 学习数据 | 专注时长、任务完成率、平均自评、连续签到天数等聚合统计 |
| 我的知识库 | 候选文档上传、导入 / 移出、重建索引、删除源文件 |

前端为原生 HTML/CSS/JS 单页应用，后端为 FastAPI，通过同域相对路径 `/api/*` 通信。

---

## 二、技术栈

| 层次 | 选型 |
|---|---|
| 工作流编排 | LangGraph（`StateGraph` + 条件路由） |
| 大模型 | LangChain + OpenAI 兼容接口（当前对接 DeepSeek） |
| 向量检索 | sentence-transformers（`paraphrase-multilingual-MiniLM-L12-v2`）+ FAISS（CPU 版） |
| 后端 | FastAPI + Uvicorn |
| 存储 | SQLite（结构化数据）+ 本地文件（FAISS 索引、文档） |
| 前端 | 原生 HTML / CSS / JavaScript + lucide 图标 |
| 部署 | Docker（`python:3.12-slim`）→ 微信云托管 |

---

## 三、目录与文件功能

### 1. 入口与工作流

- [graph.py](file:///d:/pycharm/person_demo/graph.py)
  LangGraph 工作流装配文件，核心是 `build_graph()`：
  - 注册节点：`supervisor`、`planner`、`schedule_write`、`schedule_query`、`knowledge`、`render`
  - `START → supervisor`，由 `route_after_supervisor()` 做条件路由（`plan → planner`、`schedule → schedule_query`、其余 `→ knowledge`）
  - `planner → schedule_write → render`、`schedule_query → render`、`knowledge → render`、`render → END`
  - 模块底部导出全局单例 `graph = build_graph()`，供后端与 Streamlit 直接调用

- [app.py](file:///d:/pycharm/person_demo/app.py)
  Streamlit 版本界面（早期演示入口），功能与 `web/` 一致：对话、上传文档、重建索引、展示 Agent 执行链路。与 FastAPI 版本二选一使用，不是必须启动。

### 2. 核心配置与状态

- [core/llm.py](file:///d:/pycharm/person_demo/core/llm.py)
  大模型与向量化模型配置。`get_llm()` 读取 `.env` 中的 `OPENAI_API_KEY / OPENAI_BASE_URL / MODEL_NAME` 构造 OpenAI 兼容客户端；`get_embeddings()` 用单例模式加载本地 `sentence-transformers` 模型（模型名读 `EMBEDDING_MODEL`），避免重复加载；同时设置 HuggingFace 镜像与禁用软链接以适配 Windows。

- [core/state.py](file:///d:/pycharm/person_demo/core/state.py)
  定义 LangGraph 全局状态 `AgentState`（TypedDict），字段包括 `messages（add_messages 合并器）/ user_input / route / plan_json / schedule_result / rag_context / rag_source / final_answer`，是各 Agent 之间传递数据的唯一载体。

### 3. Agent 层

- [agents/supervisor.py](file:///d:/pycharm/person_demo/agents/supervisor.py)
  调度 Agent。用提示词让大模型只输出 `plan / schedule / knowledge` 之一，`parse_route()` 做正则兜底解析，结果写入 `state["route"]`。

- [agents/planner.py](file:///d:/pycharm/person_demo/agents/planner.py)
  学习规划 Agent。要求大模型输出结构化 JSON（`goal / total_days / tasks[]`），用 `JsonOutputParser` 解析；解析失败时走纯文本二次兜底并用正则提取 JSON，产出写入 `state["plan_json"]`。

- [agents/schedule_agent.py](file:///d:/pycharm/person_demo/agents/schedule_agent.py)
  日程 Agent，含两个节点：
  - `schedule_write_node`：把 `plan_json` 落库（`save_plan`），返回「已新增一条学习计划…」
  - `schedule_query_node`：查询今日任务（`get_today_tasks`），拼装成文本清单

- [agents/knowledge_agent.py](file:///d:/pycharm/person_demo/agents/knowledge_agent.py)
  知识库 Agent。先 `retrieve()` 向量召回，再用 `RELEVANCE_PROMPT` 让大模型复核片段是否真正相关；相关则用 `RAG_PROMPT` 基于片段回答，不相关则用 `FALLBACK_PROMPT` 走大模型通用知识并显式声明「知识库中未找到相关内容」。产出 `rag_context / rag_source / final_answer`。

### 4. 数据层

- [db/sqlite_db.py](file:///d:/pycharm/person_demo/db/sqlite_db.py)
  SQLite 数据访问层。数据库路径由 `SQLITE_DB_PATH` 环境变量决定，默认 `schedule.db`。含六张表：
  - `meta`：键值配置（如记住课表订阅链接）
  - `plans`：学习计划（目标、起始日、总天数、来源提问）
  - `tasks`：每日任务（所属计划、天数、主题、内容、状态、评分、评价）
  - `checkins`：每日签到记录
  - `focus_sessions`：专注 / 休息计时记录
  - `courses`：课表课程时段（名称、星期、起止时间、地点、教师、周次、来源）
  同时负责建表、旧版单计划数据无损迁移（`init_db`）以及计划、任务、签到、专注、统计、课表的增删改查。

- [rag/ingest.py](file:///d:/pycharm/person_demo/rag/ingest.py)
  文档摄入。`DOCUMENTS_DIR`（默认 `data/documents`）只是候选文档目录，`FAISS_DIR`（默认 `faiss_index`）下的 `imported_documents.json` 记录用户明确导入的文件；`load_documents()` 只读取导入清单中的 `.pdf/.txt/.md`（PDF 走 `PyPDFLoader`，文本按 UTF-8/GBK/Latin-1 依次尝试），按 500 字 / 重叠 50 切分后生成 FAISS 索引并落盘。

- [rag/retriever.py](file:///d:/pycharm/person_demo/rag/retriever.py)
  检索层。`retrieve()` 做相似度检索并按距离阈值（默认 1.1，约余弦相似度 0.45）过滤，返回带 `rank/content/source/page/score` 的片段；`top_source()` 取最相关片段的文件名作为来源，无命中时返回「网络」。

### 5. 课表解析

- [ics_parser.py](file:///d:/pycharm/person_demo/ics_parser.py)
  iCalendar（`.ics`）解析器，独立于大模型，纯标准库实现：
  - `fetch_ics(url)`：抓取订阅链接，`webcal://` 自动转 `https://`，按响应头 / UTF-8 / GB18030 依次解码，单次上限 2MB
  - `parse_ics(text)`：处理行折叠（续行以空格或制表符开头）、转义字符（`\,` `\;` `\n` `\\`）、`DTSTART/DTEND`（支持 `TZID` 参数与 `Z` 后缀）、`RRULE` 的 `FREQ/BYDAY/INTERVAL/UNTIL`
  - 只保留「每周重复且带具体时间」的事件：`FREQ=DAILY` 等非周重复、以及没有时分秒的全天事件会被跳过
  - `BYDAY=WE,FR` 这类多日事件会展开成多条课程记录
  - 解析失败统一抛 `ICSParseError`，由接口层转为 400 响应

### 6. 后端服务

- [api/server.py](file:///d:/pycharm/person_demo/api/server.py)
  FastAPI 应用。对外提供 REST 接口，并把 `web/` 目录作为静态资源挂载（`/static`）。完整接口清单见第五节。

### 7. 前端

- [web/index.html](file:///d:/pycharm/person_demo/web/index.html)
  单页结构：侧边栏导航（AI 助理 / 完整学习计划 / 今日安排 / 我的课表 / 专注计时 / 学习数据 / 我的知识库）、对话区、各视图容器、课表导入面板与网格、上传区、确认弹窗、Toast。静态资源引用带版本号（`?v=2`）以规避浏览器缓存旧脚本。

- [web/style.css](file:///d:/pycharm/person_demo/web/style.css)
  全部样式，含主题色变量、响应式布局、计时环、日历、统计图表、课表网格等组件样式，并附带全局字号缩放段与打印隐藏规则。

- [web/app.js](file:///d:/pycharm/person_demo/web/app.js)
  前端逻辑。统一用 `api()` 封装 `fetch`（相对路径，如 `/api/chat`），负责视图切换、消息渲染、执行链路展示、任务勾选与评价、专注计时（localStorage 持久化）、签到日历、统计图表、文档上传 / 删除、课表加载 / 渲染 / 导入 / 删除。

### 8. 数据与配置

- `data/documents/`：知识库候选文档目录（当前含 `langgraph.md`）
- `data/timetable.ics`：示例课表文件，可在「我的课表」页直接上传导入
- `faiss_index/`：FAISS 向量索引持久化目录
- `schedule.db`：SQLite 数据库文件
- [.env](file:///d:/pycharm/person_demo/.env)：大模型与 Embedding 配置（含真实密钥，**不可提交 / 不可打进镜像**）
- [requirements.txt](file:///d:/pycharm/person_demo/requirements.txt)：依赖清单
- [Dockerfile](file:///d:/pycharm/person_demo/Dockerfile) / [.dockerignore](file:///d:/pycharm/person_demo/.dockerignore)：容器构建与排除规则
- [微信云托管部署指南.md](file:///d:/pycharm/person_demo/微信云托管部署指南.md)：部署到微信云托管的完整步骤

---

## 四、快速开始

### 1. 环境变量

在项目根目录创建 `.env`（或由运行环境注入）：

```ini
OPENAI_API_KEY=你的Key
OPENAI_BASE_URL=https://api.deepseek.com
MODEL_NAME=deepseek-flash
EMBEDDING_MODEL=paraphrase-multilingual-MiniLM-L12-v2
```

选填（部署 / 自定义路径时使用）：`SQLITE_DB_PATH`、`DOCUMENTS_DIR`、`FAISS_DIR`、`PORT`、`HF_ENDPOINT`、`HF_HUB_DISABLE_SYMLINKS`。

### 2. 安装依赖

```powershell
cd d:\pycharm\person_demo
py -m pip install torch --index-url https://download.pytorch.org/whl/cpu
py -m pip install -r requirements.txt
```

> 先单独装 CPU 版 torch，否则 pip 会拉取带 CUDA 的默认 wheel，体积膨胀到数 GB。

### 3. 启动后端（推荐入口，前端由后端一起托管）

```powershell
cd d:\pycharm\person_demo
py -m uvicorn api.server:app --host 127.0.0.1 --port 8000
```

需要改代码自动重载时加 `--reload`。

浏览器访问 **http://127.0.0.1:8000/** 即可打开界面。不要用 `file://` 或其它端口的静态服务器打开 `web/index.html`，否则相对路径的 `/api/*` 请求会打到错误地址，表现为「界面收不到后端数据」。

> **Windows 注意**：如果 `python` 命令不在 PATH 中，请改用 `py`（Python 启动器）。本机解释器位于 `D:\pycharm\evr\python.exe`，对应虚拟环境 `D:\pycharm\evr`。

### 4. 启动 Streamlit 版本（可选，与上面二选一）

```powershell
streamlit run app.py --server.headless true
```

### 5. 清理 8000 端口残留进程

```powershell
netstat -ano | findstr :8000
taskkill /F /PID <PID> /T
```

> 注意：若用 `--reload` 启动，uvicorn 父进程创建监听套接字后会 fork 出 worker。父进程被杀时套接字句柄被 worker 继承，端口仍被占用且运行的是旧代码。此时 `netstat` 显示的 PID 可能已不存在，必须杀掉真正的 worker 子进程（可结合 `Get-CimInstance Win32_Process -Filter "Name like '%python%'"` 定位）。

---

## 五、接口清单

### 对话与知识库

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/chat` | 接收用户输入 → `graph.invoke()` → 返回 `route / answer / trace / plan / source / sources` |
| POST | `/api/upload` | 上传为候选文档，不自动导入 |
| POST | `/api/reindex` | 重建索引 |
| POST | `/api/documents/reimport` | 重新导入全部已导入文档并重建索引 |
| POST | `/api/documents/{filename}/import` | 把候选文档加入知识库 |
| DELETE | `/api/documents/{filename}/import` | 把文档移出知识库 |
| DELETE | `/api/documents/{filename}` | 永久删除单份源文档 |
| DELETE | `/api/documents` | 清空已导入清单 |
| GET | `/api/status` | 计划概览、候选 / 已导入文档、今日任务与后续计划 |

### 计划、任务与打卡

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/plans`（兼容 `/api/plan`） | 全部计划及多级任务 |
| DELETE | `/api/plans/{plan_id}` | 删除指定计划及其关联任务 |
| PATCH | `/api/tasks/{task_id}` | 勾选完成 / 取消，完成时强制要求 1–5 分评价 |
| GET | `/api/checkins` | 签到日历 |
| POST | `/api/checkins` | 今日签到（需今日任务全部完成） |
| POST | `/api/focus-sessions` | 保存专注记录 |
| GET | `/api/focus-sessions` | 读取最近专注时间线 |
| GET | `/api/stats` | 学习数据聚合统计 |

### 我的课表

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/courses` | 全部课程时段 + 上次导入链接 |
| POST | `/api/courses` | 手动新增一条课程（校验 `HH:MM` 格式与结束时间晚于开始时间） |
| DELETE | `/api/courses/{course_id}` | 删除单条课程 |
| DELETE | `/api/courses` | 清空整张课表并忘记订阅链接 |
| POST | `/api/courses/import-url` | 从 iCalendar 订阅链接导入，整体替换上次链接导入的课程 |
| POST | `/api/courses/import-file` | 上传 `.ics` 文件导入，整体替换上次文件导入的课程 |

### 页面

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/` | 返回 `web/index.html` |
| — | `/static` | 挂载 `web/` 目录 |

---

## 六、核心执行流程

### 1. 一次对话请求的完整链路

1. 浏览器在 [web/app.js](file:///d:/pycharm/person_demo/web/app.js) 中通过 `api("/api/chat", ...)` 发送 `POST` 请求，请求体为 `{"message": "..."}`。
2. FastAPI 的 `chat()` 校验非空后调用 `graph.invoke({"user_input": message})`。
3. `supervisor_node` 调大模型判定意图，写入 `state["route"]`。
4. `route_after_supervisor()` 按路由分发：
   - `plan` → `planner_node` 生成 `plan_json` → `schedule_write_node` 落库 SQLite → `render_node`
   - `schedule` → `schedule_query_node` 查询今日任务 → `render_node`
   - `knowledge` → `knowledge_node` 检索 + 生成回答 → `render_node`
5. `render_node` 汇总为 `final_answer`。
6. `chat()` 用 `_build_trace()` 把执行链路整理成 `trace` 数组，连同 `answer / source / sources` 一起返回 JSON。
7. 前端 `addBotMessage()` 渲染气泡、来源标签和可展开的「查看处理过程」，并刷新侧边栏状态、计划与签到日历。

### 2. RAG 知识库流程

1. **上传**：`POST /api/upload` 只把文件保存到 `data/documents/`，文件显示在「可导入文档」中。
2. **导入**：用户明确点击导入后，文件名写入导入清单并全量重建索引；候选文件不会被自动摄入。
3. **摄入**：`load_documents()` 读取导入清单 → 切分为 500 字片段（overlap 50）→ `FAISS.from_documents()` 向量化 → 保存到 `faiss_index/`。
4. **检索**：`retrieve()` 取 Top-K（默认 4），同时按导入清单和距离阈值过滤。
5. **复核**：`knowledge_agent` 用大模型二次判断片段是否真正相关，避免相似技术词被误标为来源。
6. **生成**：命中则基于片段回答并标注来源；未命中则走通用知识回答并显式提示。

### 3. 计划与日程流程

1. **规划**：用户输入目标 → `planner_node` 产出 JSON → `save_plan()` 写入 `plans` 与 `tasks`，`start_date` 默认今天。
2. **查询**：`get_today_tasks()` 遍历所有计划，用 `(今天 - start_date).days + 1` 计算当前天数，取出对应 `day` 的任务。
3. **完成**：前端 `PATCH /api/tasks/{id}`，后端强制要求 1–5 分评价才允许标记完成。
4. **签到**：`check_in_today()` 校验今日任务是否全部完成，全部完成才写入 `checkins`。
5. **统计**：`get_learning_stats()` 聚合近 7 天专注时长、任务完成率、平均评分、计划数与连续签到天数。

### 4. 课表导入流程

1. **链接导入**：前端提交链接 → `fetch_ics()` 下载 → `parse_ics()` 解析 → `replace_external_courses(courses, "url", link)` 删除旧的 `source='url'` 记录并写入新记录，同时把链接存入 `meta` 表。
2. **文件导入**：前端 `FormData` 上传 `.ics` → 后端校验扩展名与编码 → 解析 → 以 `source='file'` 整体替换。
3. **手动添加**：`POST /api/courses` 以 `source='manual'` 单条写入。
4. **互不干扰**：整体替换只影响同来源记录，因此重新导入订阅链接不会冲掉手动添加的课程。
5. **渲染**：`renderCourseGrid()` 按课程最早开始 / 最晚结束自动确定时间轴范围，按课程名哈希取色，当天所在列高亮并标注「今天」，鼠标悬停色块显示删除按钮。

---

## 七、部署

项目已容器化，可直接部署到微信云托管。完整步骤、平台限制与适配说明见 [微信云托管部署指南.md](file:///d:/pycharm/person_demo/微信云托管部署指南.md)，要点：

- 容器内必须监听 `0.0.0.0:80`（本地开发用 8000）。
- 构建期已预下载 embedding 模型并预生成 FAISS 索引，避免冷启动触发 60 秒请求上限。
- 实例内存建议 ≥1GB（加载模型 + FAISS 检索较吃内存），并设置常驻实例避免冷启动。
- SQLite 与 FAISS 都是本地文件，挂载 CFS 才能跨发布持久化；`.env` 不进镜像，密钥由云托管环境变量注入。

---

## 八、常见问题

- **界面收不到后端数据**：确认后端已在 8000 端口正常启动（`netstat -ano | findstr :8000` 有 LISTENING），且通过 `http://127.0.0.1:8000/` 打开页面，而不是 `file://` 或其它端口。
- **点击侧边栏菜单没反应**：通常是浏览器缓存了旧版 `app.js`。按 `Ctrl + F5` 强制刷新；修改前端后可把 [index.html](file:///d:/pycharm/person_demo/web/index.html) 中的 `?v=` 版本号加 1 以彻底规避。
- **`python` 命令找不到**：改用 `py` 启动器，或把虚拟环境的 `Scripts` 目录加入 PATH。
- **`No module named xxx`**：依赖未装齐，执行 `py -m pip install -r requirements.txt`。Windows 下若报 `WinError 145 目录不是空的`，清理对应包目录下的 `__pycache__` 后重装。
- **端口被旧进程占用**：按第四节「清理 8000 端口残留进程」处理，注意杀掉继承套接字的 worker 子进程。
- **文档未进入问答**：手动放入或上传到 `data/documents/` 的文件只会出现在「可导入文档」；需要点击「导入」后才会参与问答。「重建已导入索引」只处理已经导入的文件。
- **课表导入失败**：确认链接 / 文件是 iCalendar 格式（`BEGIN:VCALENDAR`）；教务系统如果只能导出 Excel 或图片，需先手动添加或转换为 `.ics`。全天事件、非每周重复事件会被有意跳过。
- **首次运行较慢**：本地 Embedding 模型需要从 HuggingFace 镜像下载；已构建镜像则以镜像内模型为准。